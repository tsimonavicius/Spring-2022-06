# Config datasource (Postgres)
# ${DB_URL} - is a placeholder to read parameter from env variables
spring.datasource.url=jdbc:postgresql://${DB_URL}/eshop 
spring.datasource.username=eshop
spring.datasource.password=eshoppass
spring.sql.init.mode=never
spring.jpa.hibernate.ddl-auto=update